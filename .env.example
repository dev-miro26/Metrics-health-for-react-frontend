# Base URL of the backend API (include trailing /api/).
# Leave unset to use the built-in localhost/heroku defaults.
REACT_APP_API_URL=http://127.0.0.1:8000/api/

# Dev server port (consumed by react-scripts).
PORT=3040
